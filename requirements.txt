# TheArch — requirements
# Install with: pip install -r requirements.txt

# PDF processing
pymupdf

# OCR — torch backend required
python-doctr[torch]
torch
torchvision

# Embeddings (ingestion + retrieval share same bge-large model)
# Also used by CrossEncoder reranker (BAAI/bge-reranker-v2-m3)
sentence-transformers

# Vector store
chromadb

# Chunking
llama-index-core

# Keyword search (BM25 hybrid retrieval)
rank_bm25

# Gemini API (new SDK — google-genai, NOT google-generativeai)
google-genai

# UI
streamlit

# Auth API (server.py) backing the web/ frontend
fastapi
uvicorn[standard]
email-validator
python-multipart
python-dotenv

# Image processing (doctr + Gemini Vision + imaging pipeline)
Pillow

# Numerical
numpy

# Imaging pipeline — BioViL-T chest X-ray secondary check (optional)
# If install fails or is skipped, the pipeline falls back to Gemini Vision only
hi-ml-multimodal
