# TheArch — requirements
# Install with: pip install -r requirements.txt

# PDF processing
pymupdf

# OCR — torch backend required
python-doctr[torch]
torch
torchvision

# Embeddings (ingestion + retrieval share same bge-large model)
# Also used by CrossEncoder reranker (BAAI/bge-reranker-v2-m3)
# [onnx] extra (pulls in optimum+onnxruntime) backs the INT8-quantized
# loading path used in production when THEARCH_QUANTIZED=1 — see
# ingestion.py/retrieval.py. Local dev defaults to full-precision and never
# imports onnxruntime, but Railway's build needs the extra present.
sentence-transformers[onnx]

# Vector store
chromadb

# Chunking
llama-index-core

# Keyword search (BM25 hybrid retrieval)
rank_bm25

# Gemini API (new SDK — google-genai, NOT google-generativeai)
google-genai

# Auth API (server.py) backing the web/ frontend
fastapi
uvicorn[standard]
email-validator
python-multipart
python-dotenv

# Image processing (doctr + Gemini Vision + imaging pipeline)
Pillow

# Admission token QR code (server-generated, no third-party QR API call)
qrcode

# Numerical
numpy

# Imaging pipeline — BioViL-T chest X-ray secondary check (optional, not
# currently installed even in local dev — imaging.py already falls back to
# Gemini Vision only if it's absent). Install manually if you want it:
#   pip install hi-ml-multimodal
